[{'name': 'Request-Reply', 'type': 'RequestReply', 'implementation': 'Synchronous HTTP/REST. The Next.js frontend makes API calls to the ASP.NET Core backend for all interactive user operations.', 'applicability': 'Essential for all user-facing interactions where an immediate response is required. Fulfills core functional requirements like displaying client lists (REQ-FUNC-005), updating profiles (REQ-FUNC-008), submitting proposals, and fetching dashboard data. The performance metric REQ-PERF-001 is predicated on this pattern.'}, {'name': 'Publish-Subscribe', 'type': 'PublishSubscribe', 'implementation': 'Asynchronous messaging using RabbitMQ. The main API application publishes domain events (e.g., "SowUploaded"), which are consumed by one or more subscribers, such as the dedicated \'SowProcessingWorker\'.', 'applicability': "Decouples long-running, non-blocking tasks as required by REQ-FUNC-010 (Asynchronous SOW Processing). Also enables extensible, system-wide notifications (REQ-FUNC-025) by allowing various services to react to key events (e.g., 'ProposalSubmitted', 'ProjectCompleted') without direct coupling."}, {'name': 'File Transfer', 'type': 'FileTransfer', 'implementation': 'HTTP multipart/form-data uploads from the client to the backend API. The API layer is responsible for receiving the file stream and persisting it to a storage location before initiating processing.', 'applicability': "Directly supports the system's core data ingestion workflows: the upload of SOW documents for AI processing (REQ-FUNC-010) and the bulk import of Client and Vendor data via CSV files (REQ-NFR-009)."}, {'name': 'Circuit Breaker', 'type': 'CircuitBreaker', 'implementation': 'Implemented within the .NET Infrastructure layer, using a library like Polly, to wrap outbound network calls to external, third-party services.', 'applicability': 'Crucial for ensuring system reliability and preventing cascading failures when communicating with external APIs that can fail or experience high latency. This is essential for integrations with AWS SES (REQ-INTG-005) and the Azure OpenAI service (REQ-FUNC-013, REQ-FUNC-014).'}, {'name': 'Scheduled Task', 'type': 'ScheduledTask', 'implementation': "A background service ('DashboardMetricsCalculator') configured to run on a recurring schedule (e.g., a cron job). This service directly queries the primary database to compute and store aggregated metrics in the denormalized 'DashboardMetrics' table.", 'applicability': 'Required to implement the performance optimization tactic for the main administrator dashboard (REQ-FUNC-024). By pre-calculating and storing metrics, this pattern ensures the dashboard loads quickly, directly supporting the performance and usability goals for administrators.'}]

